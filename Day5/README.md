# 🏭 Factory Pattern (팩토리 패턴)

> **객체 생성 로직을 전담하는 별도의 공장 클래스를 두어, 객체 생성의 책임을 캡슐화하는 생성(Creational) 디자인 패턴**

---

## 1. 개요 (Overview)
Factory 패턴은 클라이언트가 `new` 연산자를 사용하여 직접 객체를 생성하지 않고, **공장(Factory) 클래스**에 객체 생성을 요청하여 필요한 객체를 전달받는 방식이다.

핵심은 **객체를 "사용하는 코드"**와 **객체를 "생성하는 코드"**를 완전히 분리하여 결합도를 낮추는 데 있다.

---

## 2. 사용 시점 (When to use)
* 생성해야 할 객체의 종류가 여러 개일 때
* 객체 생성 과정이 단순하지 않고 특정한 로직이 포함될 때
* 객체 생성 코드를 클라이언트로부터 분리하여 은닉하고 싶을 때
* 동일한 인터페이스를 구현한 다양한 객체를 조건에 따라 동적으로 생성해야 할 때
* 객체 생성 규칙이 중앙에서 집중적으로 관리되어야 할 때

---

## 3. 구조 (Structure)

### 🧱 주요 구성 요소
| 구성 요소 | 역할 |
| :--- | :--- |
| **Product** | 생성될 객체들의 공통 인터페이스 또는 추상 클래스 |
| **ConcreteProduct** | 실제로 생성되는 구체적인 제품 클래스들 |
| **Factory** | 조건(매개변수 등)에 따라 적절한 ConcreteProduct를 생성하여 반환하는 클래스 |

### 🔄 구조 흐름
1. **클라이언트**: Factory 클래스에 객체 생성을 요청 (타입 식별자 등 전달)
2. **Factory**: 전달받은 조건에 맞춰 적절한 **ConcreteProduct**를 인스턴스화
3. **반환**: 생성된 객체를 **Product** 타입(추상화된 타입)으로 클라이언트에 반환

---

## 4. 장단점 (Pros & Cons)

### ✅ 장점
* **책임 분리**: 객체 생성 로직이 한 곳에 모여 있어 관리가 용이하다.
* **낮은 결합도**: 클라이언트는 구체적인 클래스명을 알 필요 없이 인터페이스에만 의존한다.
* **중복 제거**: 여러 곳에서 사용되는 동일한 생성 로직을 Factory 내부로 통합할 수 있다.
* **유지보수성**: 생성 로직 변경 시 클라이언트 코드를 수정할 필요 없이 Factory만 수정하면 된다.

### ⚠️ 단점
* **조건 분기 증가**: 관리할 객체의 종류가 많아질수록 Factory 내부의 `if/switch` 문이 비대진다.
* **OCP 위반**: 새로운 제품이 추가될 때마다 Factory 클래스의 코드를 직접 수정해야 하므로 개방-폐쇄 원칙(OCP)을 위반할 가능성이 크다.
* **과설계**: 객체 종류가 적고 변경이 거의 없다면 오히려 구조가 복잡해질 수 있다.

---

## 5. Factory vs Factory Method
| 구분 | Factory 패턴 (Simple Factory) | Factory Method 패턴 |
| :--- | :--- | :--- |
| **생성 방식** | 하나의 공장 클래스 내에서 조건 분기로 생성 | 서브클래스가 팩토리 메서드를 오버라이딩하여 생성 |
| **OCP 만족도** | 낮음 (기존 코드 수정 필요) | 높음 (새로운 서브클래스 확장으로 해결) |
| **구조 복잡도** | 단순함 | 상대적으로 복잡함 |
| **주요 목적** | 객체 생성 책임의 단순 분리 | 생성 시점 및 방법의 유연한 확장 |

---

